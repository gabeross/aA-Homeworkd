<section>
  <div>
    <h1>Old user!</h1> <h6>Not so important anymore.</h6>
    <form class="" action=" <%= session_url %> " method="post">
      <%= auth_token %>
      <label>
      <h3>Put yer Email here in that box below.</h3>
      <input type="text" name="user[email]" value="">
      </label>
      <h3>Put yer Password in this other box. Don't tell anyone. It's a secret!</h3>
      <input type="text" name="user[password]" value="">
      <input type="submit" name="" value="One of us!">
   </form>
   <br>
   <% if flash.now[:errors] %>
   <% flash.now[:errors].each do |error| %>
   <%= error %>
   <% end %>
   <% end %>
    </form>
  </div>
</section>
